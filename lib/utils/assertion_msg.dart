// ignore_for_file: constant_identifier_names

const String SHAPE_ASSERT =
    "decoration.shape != BoxShape.circle || decoration.borderRadius == null is not true";
const String SCALE_ASSERT = "scale >= 0.0 is not true";
const String SIZE_ASSERT = "width >= 0.0 && height >= 0.0 is not true";
